stages:
  - build

build:
  stage: build
  script:
    - docker build -t tetris-project:latest .
    - docker run --entrypoint ant tetris-project all
    - pwd | ls -la

  artifacts:
    paths:
      - /app/bin/Tetris.jar
  tags:
    - dosi

